<%= form_for(@page) do |f| %>
  <% if @page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page.errors.count, "error") %> prohibited this page from being saved:</h2>

      <ul>
      <% @page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :uri %><br />
    <%= f.text_field :uri %>
  </div>
  <div class="field">
    <%= f.label :popularity %><br />
    <%= f.number_field :popularity %>
  </div>
  <div class="field">
    <%= f.label :type_names %><br />
    <%= f.text_field :type_names, data: {types: @page.types} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
